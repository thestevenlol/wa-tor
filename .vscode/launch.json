{
    "configurations": [
        {
            "type": "go",
            "request": "launch",
            "name": "Launch Go Program",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}